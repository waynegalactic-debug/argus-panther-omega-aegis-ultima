---
description: IP FORCE forensic IP pipeline — cohesive project context
alwaysApply: true
---

# IP FORCE

- **Primary account:** bmskoda@gmail.com
- **Legacy account:** waynegalactic@gmail.com (migrated)
- **Repo:** github.com/waynegalactic-debug/argus-panther-omega-aegis-ultima
- **Brand:** IP FORCE only — do not introduce parallel product names
- **Package:** v2026.07.15-IP-FORCE-ENHANCED (fully consolidated)

## Architecture

One cohesive forensic IP enforcement pipeline. Prefer `us_ipforce_*` modules.

Key entry points:
- `python3 us_ipforce.py`
- `python3 us_ipforce_monolith.py`
- `python3 us_ipforce_monolith_live.py`
- `python3 us_ipforce_deterministic_all.py run`
- `python3 court_ready_forensic_blueprint.py run`

Legacy `IP_FORCE.py` / `ip_force_*` / `aegis_*` / `us_ip_force_*` filenames are shims only.

## Conventions

- Extend existing IP FORCE monoliths; avoid parallel reimplementations.
- Data rosters are JSON in `data/` — sync via `scripts/sync_*.py`.
- Dependencies: `requirements.txt`.
- Agent branches use `cursor/<descriptive-name>-<suffix>` naming.

## Migration

See `cursor-migration/` for export/import scripts and `DATA-MANIFEST.md`.
